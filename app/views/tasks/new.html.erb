<%# poderia escrever todo esse form em uma linha, como abaixo: %>
<%# <%= render 'form', task: @task%> %>

<%= form_with model: @task do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :details %>
  <%= f.text_field :details %>
  <%= f.submit %>
<% end %>
<%= link_to "back to list", tasks_path%>
